static variable : common to all,main
data nd fun are not static in oop
static fun call another static fun without creating an obj
class variable: obj of class,each obj is created nd printed
instance variable/fun:non static,computation with a particular obj

GIT

first file is initialized to the untracked state through the add command 
the file is passed to the nxt stage ie staging state from this state we can 
ohoo
